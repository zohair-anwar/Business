# Waitlist signups — contains personal data, never commit
data/waitlist.json
data/*.json

# Dependencies (only if you later add integrations)
node_modules/

# Env / secrets
.env
.env.*
!.env.example

# Vercel local
.vercel

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vercel
